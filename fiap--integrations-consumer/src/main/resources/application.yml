spring:
  rabbitmq:
    addresses: amqp://${ACTIVE_MQ_USER}:${ACTIVE_MQ_PASSWORD}@jackal.rmq.cloudamqp.com/${ACTIVE_MQ_USER}
    url: jackal.rmq.cloudamqp.com
    password: ${ACTIVE_MQ_PASSWORD}
    user: ${ACTIVE_MQ_USER}
  datasource:
    url: jdbc:h2:mem:fiap-drone-db;MODE=MYSQL
    username: root
    password:

rabbitmq:
  queue: "queue-drone-sensorial"

